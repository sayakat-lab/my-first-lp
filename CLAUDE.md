# my-first-lp — プロジェクトのルール

## このプロジェクトについて
- HTML / CSS / JavaScript を学ぶための練習用リポジトリ
- 架空のカフェ「CAFÉ MORI」の LP（`index.html`）をベースに、章ごとに演習を追加する
- 1 章 = 1 ブランチ（例: `feature/chapter_5`、必ず最新の `main` から切る）
- 1 演習 = 1 サブフォルダ（例: `profile/`）で完結させる

## 使う技術
- HTML5 / CSS3 / 素の JavaScript（バニラ JS）
- フレームワーク・ライブラリ・ビルドツールは使わない（npm、バンドラー、CSS プリプロセッサ等なし）
- 外部 CDN やウェブフォントも原則使わず、システムフォントで組む
- 動作確認は `.html` をブラウザで直接開いて行う

## コーディング規約
- インデントは半角スペース 2
- HTML は `lang="ja"` / `charset="UTF-8"` / viewport を必ず入れる
- CSS は各ページ／演習ごとに 1 ファイル（`styles.css`）にまとめる
- JavaScript も各ページ／演習ごとに 1 ファイル（`script.js`）にまとめる
- コメントは日本語で書く
- デザインはシンプルでモダンに
- コミットメッセージ・ユーザー向けのテキストは日本語

## やってほしくないこと
- CSS / JavaScript を HTML にインラインで書く（必ず外部ファイルに分ける）
- 動作確認用の `console.log` をコミットに残す
- フレームワークや外部ライブラリを導入する
- `main` ブランチに直接コミットする（作業は必ずブランチを切る）
- 演習と関係なく既存のカフェ LP（`index.html` / `styles.css` / `script.js`）を変更する
