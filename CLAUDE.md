# my-first-lp — プロジェクトのルール

## このプロジェクトについて
- HTML / CSS / JavaScript を学ぶための練習用リポジトリ
- ビルドツールは使わない。`.html` をブラウザで直接開いて動作確認する
- 架空のカフェ「CAFÉ MORI」の LP（`index.html`）をベースに、章ごとに演習を追加する

## ファイル・ブランチ構成
- 1 章 = 1 ブランチ（例: `feature/chapter_5`）。必ず最新の `main` から切る
- 1 演習 = 1 サブフォルダ（例: `profile/`）で完結させる
- CSS は各ページ／演習ごとに 1 ファイルにまとめる（ファイル名は `styles.css`）
- JavaScript も同様に 1 ファイル（`script.js`）にまとめる。HTML にインラインで書かない

## コーディング規約
- インデントは半角スペース 2
- HTML は `lang="ja"` / `charset="UTF-8"` / viewport を必ず入れる
- コメントは日本語で書く
- デザインはシンプルでモダンに
- 動作確認用の `console.log` は残さない
- コミットメッセージ・ユーザー向けテキストは日本語
