# my-first-lp — プロジェクトのルール

## このプロジェクトについて
- HTML / CSS / JavaScript で作るリポジトリ
- 架空のカフェ「CAFÉ MORI」の LP（`index.html`）をベースに、少しずつページや機能を追加していく
- 作業のまとまりごとに 1 ブランチ（例: `feature/chapter_5`、必ず最新の `main` から切る）
- サブページは 1 つのサブフォルダ（例: `profile/`）にまとめる

## 使う技術
- HTML5 / CSS3 / 素の JavaScript（バニラ JS）
- フレームワーク・ライブラリ・ビルドツールは使わない（npm、バンドラー、CSS プリプロセッサ等なし）
- 動作確認は `.html` をブラウザで直接開いて行う

## コーディング規約
- インデントは半角スペース 2
- HTML は `lang="ja"` / `charset="UTF-8"` / viewport を必ず入れる
- CSS はページごとに 1 ファイル（`styles.css`）にまとめる
- JavaScript もページごとに 1 ファイル（`script.js`）にまとめる
- コメントは日本語で書く
- デザインはシンプルでモダンに
- コミットメッセージ・ユーザー向けのテキストは日本語

## やってほしくないこと
- CSS / JavaScript を HTML にインラインで書く（必ず外部ファイルに分ける）
- 動作確認用の `console.log` をコミットに残す
- フレームワークや外部ライブラリを導入する
- `main` ブランチに直接コミットする（作業は必ずブランチを切る）
- 作業と関係のない範囲で既存のカフェ LP（`index.html` / `styles.css` / `script.js`）を変更する
